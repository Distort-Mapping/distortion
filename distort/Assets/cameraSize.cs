using UnityEngine;
using System.Collections;

public class cameraSize : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log("Camera width: "+ gameObject.camera.pixelWidth);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
